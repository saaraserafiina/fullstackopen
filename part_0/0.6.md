sequenceDiagram

    participant browser
    participant server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa (browser sends the new note to the server)
    activate server (server processes the note, saves and updates the notes)
    server-->>browser: status code 201 Cretaed
    deactivate server

    The browser updates the notes without reloading the whole page